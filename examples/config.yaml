app:
  name: "${env:APP_NAME}-{stage}-${ps:/cfgtool/{stage}/app/name2}"
  debug: ${env:DEBUG}
  api_url: ${ps:/cfgtool/{stage}/app/api_url}
  timeout: 30
  secret: ${ps:/cfgtool/{stage}/app1/himitsu}

domain:
  api: "${stage-prefix:api}.example.com"
  web: "${stage-prefix:web}.mycompany.com"
