app:
  name: "${env:APP_NAME}-{stage}"
  debug: ${env:DEBUG}
  api_url: ${ps:{stage}/app/api_url}
  timeout: 30
  secret: ${ps:{stage}/app1/himitsu}

domain:
  api: "${stage-prefix:api}.example.com"
  web: "${stage-prefix:web}.mycompany.com"